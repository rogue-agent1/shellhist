# shellhist
Analyze shell command history (zsh/bash).
```bash
python shellhist.py                    # Auto-detect history
python shellhist.py --commands -n 30   # Top 30 commands
python shellhist.py --search "docker"  # Search history
```
## Zero dependencies. Python 3.6+.
